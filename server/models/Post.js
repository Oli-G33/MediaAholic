import mongoose from 'mongoose';

const postSchema = mongoose.Schema.create({
  userId: {
    type: String,
    required: true
  },
  userId: {
    type: String,
    required: true
  },
  userId: {
    type: String,
    required: true
  }
});
